<?xml version="1.0" encoding="UTF-8"?>
<!--  
    
There are three types of connection strings:
1) for creating a dataase
2) For opening the database  (This type is REQUIRED!)
3) For closing a database

 Most networked-based databases (MySQL, SQLServer, Oracle, etc.) only use an "open" connection string
 Derby required the three types.
  
-->
<databases>
    <db name="MySQL" driver="com.mysql.jdbc.Driver" dialect="org.hibernate.dialect.MySQLDialect">
        <connection type="open"><![CDATA[jdbc:mysql://SERVER/DATABASE?characterEncoding=UTF-8&autoReconnect=true]]></connection>
        <connection type="opensys"><![CDATA[jdbc:mysql://SERVER/]]></connection>
    </db>
</databases>
    