<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mainlist_ll"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
    >
    <LinearLayout android:id="@+id/listemptyid_ll"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
    > 
	    <TextView android:id="@+id/listemptyid_tv"
	            android:layout_width="fill_parent"
	            android:layout_height="fill_parent"
	            android:layout_weight="1"
	            android:textStyle="bold"
	            android:singleLine="true"
	            android:background="#88000000"
	            android:ellipsize="end" android:layout_gravity="center|center_horizontal" android:gravity="center_vertical|center_horizontal"/>
    </LinearLayout>   
	<ProgressBar android:id="@+id/progress"
		style="?android:attr/progressBarStyleHorizontal"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
        android:visibility="gone"
	/>
	<ListView android:id="@+id/trips"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
        android:cacheColorHint="#00000000"
        android:background="@drawable/main_list_band"
        android:tileMode="repeat" 
        android:dither="true"		
        android:visibility="gone"
	/>
</LinearLayout>

   <!--      
        // background for top LinearLayout
        android:cacheColorHint="#00000000"
        android:background="@drawable/main_list_bg"
        android:tileMode="repeat" 
        android:dither="true" 
   -->